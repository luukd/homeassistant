start_vacuum_cleaner:
  sequence:
    - service: remote.send_command
      data_template:
        entity_id: remote.harmony_hub
        command: Clean
        device: 45085387
    - delay:
        seconds: 5
    - service: remote.send_command
      data_template:
        entity_id: remote.harmony_hub
        command: Clean
        device: 45085387